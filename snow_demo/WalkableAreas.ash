struct WA {
  

  import static void Remove( int walkableAreaId ); //removes a walkable area (unless id=0) but also checks if the player is still standing on one afterwards. If not, an error message is dislayed and then the character walks to the nearest WA
  
  import static void Restore( int walkableAreaId ); //same as RestoreWalkableArea (unless id=0)
  

};

