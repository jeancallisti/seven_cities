// room script file

function room_Load()
{
  #ifdef TESTING
  if (CheckPoints.IsJumping()) { //we're in debug and we arrived in this room using the "jump to checkpoint" button
    CheckPoints.JumpDone(); 
  }
  #endif
  
  Mouse.Mode = eModeWalkto;
  PlayerUI.EnableAndShow(); 
}

function hExit_AnyClick()
{
  // Tremulas: have to add if nun lead us at the book or not
  player.ChangeRoom (35, 60, 135);    
}

function room_AfterFadeIn()
{
  if (DialogsLogic.GetVarBool("eDialog_CutSceneRuns")==1) {
    //Tremulas: Set value to 0 to avoid conflict when return to room:ConventInside
    DialogsLogic.SetVarBool ("eDialog_CutSceneRuns",false);
  } 
}
