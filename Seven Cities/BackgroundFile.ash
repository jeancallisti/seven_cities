/*
struct BackgroundFile
{
    import static void Open(String filename,  FileMode mode,  int estimatedSize);
    import static String Read(int chars);
    import static void Write(String s);
    import static void Flush(int chars);
    import static bool isFinished();
    import static int GetProgressPercent();
    
};
*/
