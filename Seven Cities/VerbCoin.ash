//start hdt            
struct VerbCoin {
    import static void Enable(bool enable);
    import static void Show(bool show,  int x = 160, int y = 100);
    import static void OnClick(GUIControl *control, MouseButton button);
};

//end hdt